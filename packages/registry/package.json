{
  "name": "@grid/registry",
  "version": "0.1.0",
  "description": "GRID Core — MiniApp Registry, sertifikasyon tetikleme, host/listing yönetimi, kill-switch, temel analytics",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/serve.js"
  },
  "dependencies": {
    "@grid/cert": "workspace:*",
    "@grid/manifest": "workspace:*",
    "@grid/pack": "workspace:*",
    "fastify": "^5.2.0"
  }
}
